# KjTabBar v1.3.6.0

## 修正内容
- 遅延したExplorer移動と後続の移動拒否・例外が重なった際、別タブのパスが上書きされる問題を修正。実際の移動元と保留中の要求を区別して選択を復旧し、遅延到着の記録を消すタイミングを修正した。
- 起動時に特殊フォルダー用Explorerの準備が失敗しても、保存された特殊タブを保持する。現在のExplorerに対応するタブへ選択を戻し、現在パスが取得できない場合は復元開始前のパスを使用する。
- タブ一覧と選択情報の一方だけ保存できた場合、保存された位置番号とパスの不一致を検出してパスで復元する。同じパスのタブが複数ある場合は、一致する位置番号を引き続き優先する。
- 設定ファイルの保存後に変更通知の購読者が例外を出しても、保存失敗としてメモリ上の設定を巻き戻さない。他の購読者への通知も継続する。
- タブ切り離しの内部結果を「未起動」「別ウィンドウ起動済み・元タブ維持」「閉じる処理の途中」「完了」に分けた。タイムアウト時に元タブを復旧する保護動作は維持する。
- 前回までの未コミット修正も含め、特殊ホスト準備中のタブ変更検出、閉じる処理の失敗時のタブ・履歴復旧、複数の遅延移動の追跡、設定保存失敗時の復旧、ドラッグ操作と保存処理の修正を収録。
- 本体バージョンを1.3.6.0、日英MSIのProductVersionを1.3.6に更新。ProductCode/PackageCodeを更新しUpgradeCodeを維持。

## 再レビュー後の追加対応
- 取消済みの移動記録は時間切れで捨てず、到着を確認した時または接続先Explorerの変更時に解除する。通常移動と遅延移動を区別できない場合は元タブの保持を優先する。
- 閉じる処理の完了待ち中は、操作前の確定したタブ一覧と選択を保存する。完了後は新しい一覧を保存し、失敗時は元に戻す。
- 起動時復元の対象・世代・生存状態を非同期待機中にも確認し、対象削除・破棄後のホスト切り替えを中断する。
- 切り離し結果を画面側で処理する。移動完了・タイムアウトを確認して保存し、元タブを残した場合は日本語/英語で案内する。
- コピー・移動の実行数を追跡し、通常のトレイ終了はUIを止めずに完了を待つ。終了待ち開始後の新しいファイル操作を受け付けない。終了済みDispatcherへの通知を抑止する。
- 配布物をビルド出力の外へ移し、ランチャーが要求するSetup.msiの名前を日英各ZIP内で維持。tools/Prepare-Release.ps1でバージョン・内包EXE・ハッシュを検査する。

## 検証
- Releaseビルドおよび全443テスト成功。139,968操作ケース、長時間遅延、保存確定前後、復元待機中の対象削除・破棄、切り離し完了・タイムアウト、ファイル操作の終了待ちを含む。
- 日英MSIから抽出したEXEそれぞれで全443テスト成功。日本語EXE SHA-256: 4ACAC600FCD3AE4842137DCAFCC25D0DF8EE8A17BB5C80F5CEDDD531B6D4699C。英語EXE SHA-256: 3B3966570EFFF348340CAEC2F7DE7C81881F1A8F36C56EFE285ECB59B17CA46D。
- 日英MSIのビルド成功。配布ZIP内のsetup.exe/Setup.msiの組と全配布ファイルのSHA-256を検証。最終検証の証跡はartifacts/release136-*および配布先のSHA256SUMS.txtに記録。
- 英語版ビルドで一度TabBarWindow.baml欠落が発生。通常ソリューションの再ビルド後に再実行し成功。
- 検証専用ファイルでWindowsのSHFileOperationによるコピー・移動がともに戻り値0で完了。内容一致、移動元消失、操作の終了待ちを確認。実タブバーからのドラッグ操作・終了操作は未検証。
- 既存ファイルのUTF-8/BOM有無・CRLFを維持。git diff --check成功。

## 配布物
保存先: artifacts/releases/v1.3.6.0/

- KjTabBar-v1.3.6.0-ja.zip（日本語）、KjTabBar-v1.3.6.0-en.zip（英語）。展開してsetup.exeを起動する。
- ja/ と en/ にはそれぞれsetup.exe、Setup.msi、LICENSEを同梱する。
- 単体EXE、設定ファイル、LICENSE、SHA256SUMS.txtも保存する。

## 検証の限界
- 実Explorerの画面操作、インストール・更新導入、長時間運用は未検証。MSIの管理用展開は通常インストールではない。
- タスクマネージャーによる強制終了・OS終了・電源断でのファイル操作の完遂は保証しない。通常のトレイ終了を対象に完了待ちを追加した。
- 未確定の閉じる処理中に終了した場合は、安全側として操作前の一覧を次回復元する。
- 切り離し先を開いた後に移動が失敗した場合、元タブも保持し案内する。
- 性能向上は実測していない。
