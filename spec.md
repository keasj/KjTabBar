# KjTabBar 仕様書（実装準拠版）

## 1. 概要
KjTabBar は、Windows エクスプローラー（`explorer.exe`）をタブ化し、単一のウィンドウに統合する常駐型 WPF アプリケーションである。  
本アプリはエクスプローラー本体を拡張するのではなく、独立した透明ツールウィンドウを上部に重ねて表示する。

## 2. 動作環境
- OS: Windows 10 / Windows 11
- 言語・ランタイム: C# 7.3 / .NET Framework 4.8.1
- UI: WPF

## 3. 構成
- `App.xaml.cs`
- 単一起動制御、エクスプローラー監視、ウィンドウ吸収、タブ永続化、タスクトレイ管理を担当
- `Views/TabBarWindow.xaml(.cs)`
- タブバー表示、入力イベント、ドラッグ＆ドロップ、表示位置追従を担当
- `ViewModels/TabBarViewModel.cs`
- タブ状態管理、アクティブタブ切替、エクスプローラー同期を担当
- `Models/ExplorerManager.cs`
- Shell COM / Win32 API を用いたパス取得・ナビゲーション・選択項目復元を担当
- `Models/UserSettings.cs`
- ユーザー設定のロード/保存（`settings.xml`）を担当

## 4. 起動と常駐
### 4.1 単一起動
- `KjTabBar_Application_Mutex` により多重起動を禁止する。

### 4.2 標準ユーザー権限での実行保証
- 特権（管理者）権限で起動された場合、エクスプローラー（標準ユーザー権限）とのドラッグ＆ドロップが制限されるため、自分自身を `explorer.exe` 経由で再起動することで標準ユーザー権限へ降格させる。
- 再起動時は多重起動チェックをパスし、無限再起動を防ぐために `--kjtb-shell` 引数を使用する。

### 4.3 スタートアップ登録
- インストール時にセットアップカスタムアクションで `HKCU\Software\Microsoft\Windows\CurrentVersion\Run` の `KjTabBar` を確認し、実行ファイルパスを登録する。
- 通常のアプリ起動時にはスタートアップ登録を変更しない。
- 登録失敗時もインストールは継続する。

### 4.4 タスクトレイ
- 起動後は通知領域に常駐し、メニュー `終了` からアプリ終了が可能。
- アイコンにカーソルを乗せた際（ホバー時）は、アセンブリ属性から動的に取得したプログラム名（`AssemblyTitle`）とバージョン（`AssemblyVersion`）を `KjTabBar v1.2.8.0` の形式でツールチップとして表示する。

### 4.5 監視・保守周期
- **エクスプローラー監視**: 1秒間隔タイマーでエクスプローラー一覧とタブバー対応を監視する。
- **メモリ保守**: 3分間隔で COM キャッシュクリアと未使用 COM オブジェクトのクリーンアップを行う。UI 停止を避けるため、明示的なフル GC は実行しない。

### 4.6 COM スレッド運用
- 現状の Shell COM 呼び出しは、UI スレッドと `ComThreadService` の専用 STA ワーカースレッドの両方から実行される。
- `Shell.Application` は `[ThreadStatic]` キャッシュとしてスレッドごとに保持し、メモリ保守周期では UI スレッド側と COM ワーカー側の両方でキャッシュ解放を試みる。
- `ComThreadService` は最大64件の有界キューを使用し、各呼び出しは5秒でタイムアウトさせて呼び出し元の無期限待機を防ぐ。
- キャッシュの生成・解放時はスレッド ID をログへ記録し、長期稼働時の COM ライフサイクルを追跡できるようにする。
- 将来的に大きな設計変更を行う場合は、Shell COM 呼び出しを `ComThreadService` の専用 STA ワーカーへ段階的に集約する。ただし、同期 API と UI 更新経路への影響が大きいため、一括移行は行わない。

## 5. エクスプローラー監視と取り込み
### 5.1 監視対象
- `EnumWindows` で可視ウィンドウを列挙し、クラス名 `CabinetWClass` をエクスプローラーとして扱う。

### 5.2 タブバーの生成
- 有効な既存タブバーがない場合、新規エクスプローラーに対して `TabBarWindow` を 1 つ作成する。
- 常にシステム全体で **最大 1 つのタブバーウィンドウ** のみが存在することを保証する。
- 新規エクスプローラー処理は `HWND` ごとに直列化し、同一ウィンドウの重複処理や並行判定によってタブバーが複数生成されないようにする。
- 作成時に `%APPDATA%\KjTabBar\tabs.txt` を読み込み、DPAPI で保護された保存形式または旧来の平文保存形式から復元可能タブを適用する。

### 5.3 新規エクスプローラーの取り込み判定
- **単一タブバーの原則**: アプリケーションが管理する（タブバーを表示する）エクスプローラーは常に 1 つのみとする。
- **デスクトップ起点の吸収**: デスクトップ上のフォルダ、ショートカット、特殊項目（PC/ネットワーク/ごみ箱等）から開かれたと判定されたエクスプローラーは、既存のタブバーへ吸収し、タブとして追加する。吸収対象の判定は、デスクトップ配下のパス、デスクトップ上のショートカットのリンク先、デスクトップ上のシェルアイテム、または特殊シェルパス（PC/ネットワーク/ごみ箱）への該当に基づく。
- **同一パスの扱い**: デスクトップ起点で明示されたパスは、保存済みタブに同一パスが存在する場合でも、新しいタブとして追加して選択する。重複タブを許可する。
- **通常起動の独立**: タスクバー、スタートメニュー、または他のアプリから起動された新規エクスプローラーウィンドウは、吸収せず、タブバーなしの通常ウィンドウとしてそのまま残す。
- **判定の仕組み**: 前景ウィンドウの遷移（デスクトップからの遷移を `IsDesktopCandidate` で推定）およびパスの物理照合（デスクトップ配下か、デスクトップ上のショートカットか等）により判断する。
  - **通常フォルダの判定**: デスクトップからの遷移推定（`IsDesktopCandidate`）が True の場合に限り、「デスクトップ配下か」「デスクトップ上のショートカットか（`IsDesktopShortcutTarget`）」などの物理照合を行い、一致すれば吸収する。「別ウィンドウで開く」操作時は `IsDesktopCandidate` が False となるため、絶対に吸収せず独立させる（デグレ防止ルール）。
  - **特殊シェル項目（PC / ネットワーク / ごみ箱）**: これらの特殊 GUID パスも `IsDesktopCandidate` が True の場合のみ吸収対象とする。パスが一致するだけでは吸収根拠にせず、タスクバーやスタートメニューから開いた通常起動は独立させる。
  - **重要（特殊パスの特例吸収）**: 「コントロールパネル」や「プログラムと機能」等の特殊ショートカットは、プロセス分散等の影響で起動まで時間がかかり、前景推定（`IsDesktopCandidate`）が強制的に時間切れ（False）になることがある。このため、コントロールパネル系のパスに限り、`EVENT_OBJECT_SHOW` でデスクトップ起点候補として一度非表示化されたウィンドウに対してのみ、「それがデスクトップ上のショートカット（.lnk）から開かれたものか」を追加で物理評価し、合致すれば吸収する仕様とする。デスクトップ起点の痕跡がない通常起動ウィンドウは、デスクトップ上に同じショートカットが存在しても吸収しない。
    - ※注意点: コントロールパネルのCOMは末尾にビューステート（`\0`等）を付加してくるため、この特例評価での文字列照合では必ず末尾の `\[数字]` を除去して正規化してから照合すること。また、ウィンドウタイトルから推測した汎用パス（`::{21EC...}` ）で、COMから取得できた本来のパス（`::{26EE...}` ）を上書きしてしまわないよう注意する。
- **非表示化**: 吸収対象の可能性が高い（デスクトップ起点と推定される）新規エクスプローラーは、`EVENT_OBJECT_SHOW` フックにより即座に非表示化し、吸収完了まで画面に表示しない（ちらつき防止）。判定の結果、吸収対象外となった場合は即座に再表示する。
- **デスクトップショートカット解決キャッシュ**: デスクトップ上の `.lnk` 解決は COM を使用するため、ショートカットのパス、更新時刻、サイズが変わらない限り解決結果を再利用する。これにより大量のショートカットや遅いリンク先がある環境で、`ComThreadService` の単一 STA ワーカーが同じ解決処理で繰り返し詰まることを抑制する。

### 5.3.1 `ProcessNewExplorerWindowAsync` のステートマシン仕様

新規エクスプローラーウィンドウの処理は `ProcessNewExplorerWindowAsync(hwnd, validTarget)` で行い、`HWND` ごとに 1 件だけ実行中を許可する前提で、以下の直列フローで判定する。

```
入力: hwnd（新規ウィンドウ）, validTarget（既存タブバーVM、なければ null）

Step 1: パス取得
  path ← ExplorerManager.GetCurrentPath(hwnd)     // COM 経由。遅延あり
  titlePath ← GetDesktopVirtualPathFromWindowTitle(hwnd) // ウィンドウタイトルから仮想パスを推定

Step 2: コントロールパネル即時確定（タイトル優先）
  if titlePath が CP → path = AllControlPanelPath に強制上書き

Step 3: パス解決済み判定
  decisionPath = path
  if decisionPath が empty/null → リトライカウント加算、上限到達なら
    - validTarget あり → 吸収（パス不明時は「PC」等へフォールバック検討）
    - validTarget なし → CreateNewTabBar
    上限未到達 → return（次の Tick で再実行）

Step 4: 実行
  - validTarget なし → CreateNewTabBar（新規タブバー作成）
  - validTarget あり → デスクトップ起点判定に基づき吸収可否を決定
    - 吸収対象 → AbsorbNewExplorer（タブとして追加し、元ウィンドウを閉じる）
    - 吸収対象外 → IgnoredWindows に追加（タブバーなしの通常ウィンドウとして残す）
```

### 5.3.2 Windows 11 固有の COM 遅延挙動
- **COM パス取得の遅延**: `ShellWindows` COM オブジェクトの初期化が遅れ、`GetCurrentPath` が最初の数 Tick で `null` を返すことがある。このため標準リトライ機構（最大 16 Tick = 8 秒）を設けている。
- **ウィンドウタイトルの先行更新**: ウィンドウタイトルは COM パスより先に更新されるため、タイトルからの即時確定を優先する。

### 5.4 特殊パスの扱い
- **コントロールパネル**: 
  - コントロールパネル本体は `::{21EC2020-3AEA-1069-A2DD-08002B30309D}` に正規化する。
  - 個別項目（プログラムと機能等）もタブとして保持可能。
- **ホーム / PC / ネットワーク / ごみ箱**: これらも特殊 GUID パスとして管理する。

### 5.5 多言語対応
- `SHParseDisplayName` / `SHGetNameFromIDList` API を用いて OS のローカライズ済み表示名を動的に取得する。

## 6. タブ仕様
### 6.1 基本状態
- `TabBarViewModel` 生成時、対象エクスプローラーの現在位置から初期タブを 1 件生成する。

### 6.2 追加・同名対処
- 同一パスのタブも複数開くことが可能である（以前あった統合設定は廃止）。
- タブの表示名は原則として現在のフォルダ名とする。
- 複数のタブが同名のタイトルになった場合は、上位のフォルダ名を付加して（例: `親フォルダ\現在のフォルダ`）区別を試み、それでも重複する場合は先頭に (1)〇〇, (2)〇〇 のように連番を付与して区別する。
- タブ名が一定の長さ（30文字）を超える場合、パスの構造を維持しつつ中間を省略（「先頭...末尾」形式）して表示する。

### 6.3 新規タブ追加ボタン
- タブバーの末尾にある「＋」ボタンでシェルのフォルダ選択ダイアログを開き、新しいタブを追加できる。
- 選択ダイアログは通常フォルダに加え、PC / ネットワーク / コントロールパネル / ごみ箱 / ギャラリー等の特殊フォルダ選択に対応する。

### 6.9 タブアイコン表示
- 各タブの先頭に、そのタブのパスに対応する小アイコンを表示する。
- 通常フォルダはパスベースで取得し、特殊フォルダ（`::{...}` / `shell:`）は PIDL ベースでシェル固有アイコンを取得する。
- コントロールパネル配下項目は、通常の GUID 解決で取得できない場合にコントロールパネル配下コンテキスト付きのシェルパスで再取得し、親アイコン化を回避する。

### 6.4 選択とナビゲーション
- タブ選択時は `ExplorerManager.Navigate` で対象パスへ遷移する。

### 6.5 クローズ
- 全タブを閉じた場合は、規定のパスで新規タブを 1 件作成し、最低 1 タブを維持する。

### 6.6 並び替え
- ドラッグ＆ドロップでタブ順序を入れ替え可能。

### 6.7 存在しないパスの処理
- タブクリック時に、そのパス（フォルダ）が既に存在しない場合、自動的にそのタブを閉じ、ナビゲーションを中止する。
- 特殊パス（コントロールパネルやホーム等）は存在チェックの対象外とし、常に維持される。

### 6.8 閉じたタブの復元（アンドゥ）
- 閉じたタブの履歴を最大 50 件保持する。
- 右クリックメニュー「閉じたタブを開く」から、直近で閉じたタブを元のインデックス位置に復元できる。
- 「右側のタブを閉じる」や「左側のタブを閉じる」などで複数のタブを一括で閉じた場合、一度のアンドゥ操作でそれらすべてを元の並び順で一括復元できる。
- タブバーの背景部分の右クリックメニューからも復元が可能。

## 7. ドラッグ＆ドロップ
### 7.1 タブ移動
- タブ要素の左ドラッグで並び替える。
- タブ要素をタブバーの外へ左ドラッグし、エクスプローラーウィンドウ外でドロップした場合は、そのタブのパスで新規エクスプローラーを別ウィンドウとして開き、元のタブは閉じる。

### 7.2 外部ファイル/フォルダのドロップ
- タブ上へのドロップ: 対象のタブが示すフォルダパスに対して、ドラッグしたファイル・フォルダのコピーまたは移動操作を行う。
  - 右ドラッグでドロップした場合: 「ここにコピー」「ここに移動」などを選べるコンテキストメニューを表示する。
  - 左ドラッグでドロップした場合: 同一ドライブなら移動、別ドライブならコピーする（CtrlやShiftキーによる操作変更にも対応）。
- 背景部分（または未割り当てパスのタブ）へのドロップ: フォルダの場合は新規タブとして挿入・追加する。ファイルの場合は無視する。
- 右ドラッグメニューの「シンボリックリンクをここに作成」は、現在の通常ユーザー権限で作成を試みる。権限不足で失敗した場合は管理者権限で再試行せず、Windows の開発者モードを有効にする、または権限のある作成先を指定するよう案内を表示する。

### 7.3 外部エクスプローラーウィンドウの吸収（ドロップ合流）
- タブバーを持たない独立したエクスプローラーウィンドウをドラッグし、既存のタブバー領域の上に重ねてドロップ（マウスボタンを離す）した際、そのウィンドウの現在のパスを取得して既存のタブバーに新規タブとして追加（吸収）し、元のエクスプローラーウィンドウを閉じる。
- 判定には Win32 API の `EVENT_SYSTEM_MOVESIZEEND` フックを使用し、移動終了時のマウスカーソルがタブバーの物理座標範囲内にあるかを検証して吸収を行う。

## 8. UI 表示仕様
### 8.1 ウィンドウ形態
- タブバーはエクスプローラーウィンドウの上部にツールウィンドウとして追従する。
- `Owner` に対象エクスプローラーの `HWND` を設定し、連動させる。

### 8.2 位置・DPI 追従
- エクスプローラーの上端へ追従配置し、DPI 変更時は即座に補正する。

### 8.3 フォーカス制御
- 操作後は `ForceSetForegroundWindow` でエクスプローラーにフォーカスを戻す。

### 8.4 フォント
- `FontFamily` / `FontSize` 等を `UserSettings` から適用する。

### 8.4 右クリックメニュー
- **タブ右クリック**: `タブの複製`, `別ウィンドウで開く`, `パスのコピー`, `タブを閉じる`, `右側のタブを閉じる`, `左側のタブを閉じる`, `閉じたタブを開く`。
  - 「右側/左側を閉じる」は、対象タブの左右にタブが存在する場合のみ有効。
  - 「閉じたタブを開く」は、履歴が存在する場合のみ有効。
- **背景右クリック**: `閉じたタブを開く`, `設定...`。
  - 「閉じたタブを開く」は、履歴が存在する場合のみ有効。

### 8.5 テーマ（ダークモード対応）
- Windows のアプリモード設定（AppsUseLightTheme）に連動する。

### 8.6 ツールチップ（ホバー表示）
- タブにカーソルを合わせた際、そのタブが示す元のフルパスを表示する。

## 9. 設定と保存データ
### 9.1 設定ファイル
- パス: `%APPDATA%\KjTabBar\settings.xml`
- 項目: `FontFamily`, `FontSize`, `IsBold`, `IsItalic`
- ※ `AbsorbNewExplorer`（吸収設定）は廃止され、常に有効となる。

### 9.2 タブ保存ファイル
- パス: `%APPDATA%\KjTabBar\tabs.txt`
- DPAPI を用いて、現在の Windows ユーザー単位で保護したタブパス一覧を保存する。旧来の平文保存形式も読み込み互換として受け入れる。

## 10. アンインストール時クリーンアップ
- セットアップカスタムアクションで `Run` 関連の起動登録レジストリのうち、インストール済み実行ファイルと一致するエントリだけを削除する。
- `%APPDATA%\KjTabBar` 配下の `settings.xml` および `tabs.txt` は削除しない。

## 11. 制約事項・補足
- **単一タブバー構成**: 本アプリは常に一つのタブバーウィンドウですべてのエクスプローラーを管理する仕様であり、独立した複数のタブバーを持つことはできない。
- **エクスプローラーの統合条件**: 新規ウィンドウのうち、既存タブバーが存在する状態でデスクトップ起点と判定されたものだけが既存タブバーへ統合される。タスクバー、スタートメニュー、他アプリ等からの通常起動は独立ウィンドウのまま保持する。
- **アンインストール後のユーザーデータ**: 設定ファイル（`settings.xml`）および DPAPI 保護されたタブ保存ファイル（`tabs.txt`）は削除せず保持する。

## 12. 例外処理・ログ方針
- 例外を握りつぶして処理継続する場合でも、原則として無言 `catch` は避け、少なくともログへ記録する。
- 高頻度に発生し得る COM / Shell 列挙失敗は、ログ肥大化を避けるため抑制付きログを許可する。
- 低レベル COM ヘルパー（COM プロパティ取得・メソッド呼び出し）は、呼び出し頻度と上位ログ重複を避けるため、失敗を `null` で返してよい。ただし、ユーザー操作や状態判定に影響する上位処理では、必要に応じて抑制付きログを残す。
- セットアップ後処理の PowerShell 内ログ関数は、ログ出力失敗でインストールを失敗させないため、ログ関数自身の例外のみ無言で握りつぶしてよい。ショートカット作成、SID 解決、Run キー登録などの実処理失敗は、機微なパスや SID をそのまま残さない形でログへ記録する。
- ユーザーが明示的に実行した操作（設定保存、別ウィンドウ起動、ショートカット作成、ファイル操作など）が失敗した場合は、ログだけでなく必要に応じて UI 上でも失敗を通知する。
- フォールバック継続が目的の例外処理では、機能継続を優先してよいが、失敗理由が追跡不能にならないようにする。
