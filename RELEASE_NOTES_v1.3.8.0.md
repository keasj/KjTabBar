# KjTabBar v1.3.8.0

## 変更内容

- デスクトップ上のフォルダ、特殊項目、またはそれらへのショートカットを開いた場合、同じ場所の既存タブを選択して前面に表示する。一致するタブがなければ新しいタブを追加する。
- 通常フォルダに加え、コントロールパネル本体・配下項目、PC、ネットワーク、ごみ箱などの特殊フォルダに対応する。表示名ではなく場所を照合し、異なるコントロールパネル項目は区別する。
- 同じ場所のタブが複数ある場合は選択中の一致タブを優先し、それ以外は左端を選択する。既存の重複タブは削除しない。
- Windows が既存ウィンドウを再利用した場合も、移動前のタブを保持して起動先のタブを選択・追加する。デスクトップ起動時の保存タブ復元にも再利用規則を適用する。
- タブの複製、ドラッグ追加、独立したウィンドウのドラッグ吸収、＋ボタンでは、同じ場所でも新しいタブを作成する。
- デスクトップ以外からの起動や、タブ操作に伴う内部ウィンドウ生成には再利用規則を適用しない。
- 本体を 1.3.8.0、日英 MSI を 1.3.8 に更新。ProductCode と PackageCode を更新し、UpgradeCode を維持する。

## 検証

- Release ビルド成功。ソースからの自動テスト 474 件すべて成功。
- 日本語・英語のセットアップ単体ビルド成功。各 MSI から抽出した本体でも、それぞれ 474 件すべて成功。
- 両本体の AssemblyVersion・FileVersion 1.3.8.0 と TabBarWindow の WPF 画面リソースを確認。
- 両 MSI の ProductVersion 1.3.8、言語 1041／1033、ProductCode の更新、UpgradeCode の維持、終了処理順序 1399 < InstallValidate 1400 を確認。
- 実際の MSI PackageCode: 日本語 {197F9266-45A9-4508-804F-79B598A7311E}、英語 {5FCF4906-46C1-4B18-88D2-959B3EAB9D23}。
- 両 ZIP の setup.exe・Setup.msi・LICENSE と、配布物 11 件の SHA-256 マニフェストを検証。
- セットアップ初回ビルドで WPF 生成ファイル不足が発生したため、本体を再構築してセットアップを再実行し、最終的に両言語とも成功した。
- 公開前の再検証で、サンドボックス内のシェル登録照会テストが失敗した。通常の Windows 環境で再実行し、474 件すべて成功した。
- 実際のデスクトップ操作、通常インストール・更新、長時間運用は未検証。

## 配布物

- artifacts/releases/v1.3.8.0/KjTabBar-v1.3.8.0-ja.zip
- artifacts/releases/v1.3.8.0/KjTabBar-v1.3.8.0-en.zip
- ZIP 内の setup.exe と Setup.msi を同じフォルダに展開して使用する。
