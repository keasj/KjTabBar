#!/bin/sh

repo_root=$(git rev-parse --show-toplevel)
powershell.exe -NoProfile -ExecutionPolicy Bypass -File "$repo_root/tools/Check-LineEndings.ps1"
status=$?

if [ $status -ne 0 ]; then
  echo "pre-commit: blocked due to line ending policy violations."
  exit $status
fi
