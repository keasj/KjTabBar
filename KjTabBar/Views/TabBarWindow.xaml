<Window x:Class="KjTabBar.Views.TabBarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:KjTabBar.ViewModels"
        WindowStyle="None"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        AllowDrop="True"
        Background="Transparent"
        AllowsTransparency="True"
        SizeToContent="Height"
        Width="800"
        FontFamily="{Binding FontFamily}"
        FontSize="{Binding FontSize}"
        FontWeight="{Binding FontWeight}"
        FontStyle="{Binding FontStyle}"
        Icon="pack://application:,,,/KjTabBar;component/Assets/Icons/app_icon.png"
        Visibility="{Binding WindowVisibility, Mode=TwoWay}">

    <Window.Resources>
        <Style x:Key="TabCloseBtn" TargetType="Button">
            <Setter Property="Width" Value="16"/>
            <Setter Property="Height" Value="16"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeFgSubtle}"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="3" Width="16" Height="16">
                            <TextBlock Text="✕" FontSize="8" Foreground="{TemplateBinding Foreground}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeCloseHoverBg}"/>
                                <Setter Property="Foreground" Value="White"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="AddTabBtn" TargetType="Button">
            <Setter Property="Width" Value="28"/>
            <Setter Property="Height" Value="24"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Foreground" Value="{DynamicResource ThemeFgSubtle}"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd" Background="{TemplateBinding Background}"
                                CornerRadius="4">
                            <TextBlock Text="＋" FontSize="13" Foreground="{TemplateBinding Foreground}"
                                       HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Bd" Property="Background" Value="{DynamicResource ThemeTabHover}"/>
                                <Setter Property="Foreground" Value="{DynamicResource ThemeAccent}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

    </Window.Resources>

    <Border Background="{DynamicResource ThemeWindowBg}"
            BorderBrush="{DynamicResource ThemeBorderLine}"
            BorderThickness="1,1,1,0"
            CornerRadius="8,8,0,0"
            MouseRightButtonUp="Border_MouseRightButtonUp"
            PreviewMouseWheel="TabBarWindow_PreviewMouseWheel">
        <ItemsControl x:Name="TabItemsControl" Focusable="False" VerticalAlignment="Center" Margin="4,2,4,4" MinHeight="30">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>
            <ItemsControl.Resources>
                <DataTemplate DataType="{x:Type vm:TabItemViewModel}">
                    <Border x:Name="TabBorder" CornerRadius="6,6,0,0" Padding="10,4,4,4" Margin="1,2,1,0" MinWidth="80" MaxWidth="500" Cursor="Hand" Background="{DynamicResource ThemeWindowBg}" BorderBrush="Transparent" BorderThickness="1,1,1,0" MouseLeftButtonUp="Tab_Click" PreviewMouseLeftButtonDown="Tab_MouseLeftButtonDown" PreviewMouseMove="Tab_MouseMove" MouseRightButtonUp="TabBd_MouseRightButtonUp" ToolTip="{Binding Path}">
                        <DockPanel>
                            <Button Style="{StaticResource TabCloseBtn}" Margin="6,0,0,0" Click="CloseTab_Click" DockPanel.Dock="Right" />
                            <TextBlock Foreground="{DynamicResource ThemeFgNormal}" VerticalAlignment="Center" TextTrimming="CharacterEllipsis" Text="{Binding Title}" />
                        </DockPanel>
                    </Border>
                    <DataTemplate.Triggers>
                        <Trigger Property="IsMouseOver" Value="True" SourceName="TabBorder">
                            <Setter TargetName="TabBorder" Property="BorderBrush" Value="{DynamicResource ThemeAccent}"/>
                        </Trigger>
                        <DataTrigger Binding="{Binding IsActive}" Value="True">
                            <Setter TargetName="TabBorder" Property="BorderBrush" Value="{DynamicResource ThemeActiveTabBorder}"/>
                        </DataTrigger>
                    </DataTemplate.Triggers>
                </DataTemplate>
            </ItemsControl.Resources>
        </ItemsControl>
    </Border>
</Window>

