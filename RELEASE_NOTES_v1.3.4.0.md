# KjTabBar v1.3.4.0

## 変更内容
- コントロールパネルの復元中は初期ホームとタブバーの先行表示を抑制し、準備完了後に表示。準備に失敗した場合は元のウィンドウを再表示する。
- コントロールパネルの終了時に Explorer が異常終了して次の起動が遅れる現象への対策として、コントロールパネルのホストにはタブバーの Win32 owner を設定せず、位置と前後関係を追従させる。
- 最大化・最小化前の状態と通常サイズの位置を保持し、再表示やホスト切り替え時に復元。一時退避の画面外座標が復帰先に残る問題を修正。
- 既存タブバーがない場合は表示通知から取り込みを予約し、場所を取得できなければ100ミリ秒後に一度だけ再確認。監視タイマーの通知を UI の入力待ちから独立させ、重複通知をまとめる。
- 内部ホストの起動確認を100ミリ秒間隔・最大80回へ拡張し、追跡期限を10秒に変更。元ウィンドウが閉じられた場合は待機を中止する。
- デスクトップから同じフォルダーを再度開き、既存ウィンドウが再利用された場合にもタブを追加。PCやフォルダーの再起動操作で、最小化前の最大化状態を復元する。
- 通常フォルダーへの切り替え時は適切なホストを準備し、再利用されたホストを誤って閉じないよう修正。連続したコントロールパネル起動でも元のフォルダーホストとの対応を維持する。
- 明示した起動先がある場合、保存タブの復元による先行ナビゲーションを抑制する。
- 再表示時に枠の再計算を一度要求し、右上のウィンドウ操作ボタンが表示されない現象に対処。退避位置から戻す際には再描画も要求する。
- 名前から場所を確定できる場合は不要な Shell 問い合わせを省略。対象ウィンドウの閉鎖時には場所取得中のワーカー待機を中止する。
- 診断を有効にした場合の起動・終了・位置復元・監視処理の計測を追加。
- 本体を1.3.4.0、日英インストーラーを1.3.4へ更新。ProductCode / PackageCode を更新し、UpgradeCode を維持。

## 検証結果
- dotnet build KjTabBar.build.sln -c Release 成功（警告0・エラー0）。Visual Studio MSBuild による Release 再ビルドも成功。
- vstest.console.exe による全371件のテスト成功。
- 日英インストーラーの Release ビルド成功。英語版の初回は WPF 生成ファイル不足で失敗したため、手順書に従いアプリを再ビルド後に再実行し、成功2・失敗0を確認。
- 日英 MSI から展開した実行ファイルで、それぞれ全371件のテスト成功。
- 日英 MSI の ProductVersion=1.3.4、内包 EXE の FileVersion=1.3.4.0 を確認。
- 日英 MSI の終了処理パッチ適用を確認。KjTabBarCloseRunningProcess=1399、InstallValidate=1400。

## 配布物
保存先: Setup/Release/v1.3.4.0/

- 本体 EXE、日本語セットアップ EXE / MSI、英語セットアップ EXE / MSI の計5ファイル。
- 日本語 MSI PackageCode: {73A84F1A-E2CF-48CD-A688-4FC386F8465E}
- 英語 MSI PackageCode: {8C9BFB81-720C-4740-941F-1CE446F381E9}
- 日本語 MSI 内包 EXE SHA-256: AB2076E085E8E429F3578DD4985EE9DE2B8202D1F2C5DC329759B8C035294C62
- 英語 MSI 内包 EXE・単体配布 EXE SHA-256: 12CA836A94DAC8DF22038AE3674825DD18CA624E2679786BA4B0B07D9C74493C
- 日英の内包 EXE はビルド過程によってハッシュが異なるため、それぞれを検証対象とする。

## 未検証・残る確認事項
- 今回生成した最終配布物での実 Explorer の操作、長時間利用、インストール済みアプリの更新と旧版からのアップグレード。MSI は検証用フォルダーへ展開したのみ。
- 既存の調査記録では、owner変更の比較版で12回の内部復元とユーザー操作の改善を確認済み。ただし、最終版における瞬間的な先行表示や右上ボタン非表示の再発が、全条件で解消したとは未確認。
