# KjTabBar v1.3.7.0

## 修正内容
- Explorer吸収時に移動失敗でも元ウィンドウを閉じていた問題を修正。移動先への到着を確認してから閉じ、失敗・タイムアウト時は元ウィンドウを保持する。
- 背景ドロップでも特殊フォルダー用ホストを準備する。複数件の追加は順に処理する。
- 同一ドライブへの既定ドロップを移動へ戻した。別ドライブはコピーし、修飾キーと許可された操作を尊重する。
- 右ドラッグのコピー・移動・リンク作成先をドロップ時に固定し、後から変化したタブのパスを使わない。
- アイコン要求の合流・順次投入・一時失敗時の再試行を追加。失敗中は既定アイコンを表示し、古い取得結果は適用しない。
- UIからのShellパス取得・移動・選択復元を非同期化。待機中のタブ削除、選択変更、ホスト変更、終了を確認してから結果を適用する。
- ショートカット・シンボリックリンク作成もUIを止めず実行し、通常終了時のファイル操作待ちの対象とする。
- Ctrl＋ホイールの設定保存失敗時に値を戻し、エラーを通知する。
- タブ一覧と選択を単一の暗号化スナップショットとして保存。従来形式は互換コピーとして維持し、旧データの読み込みにも対応する。
- 本体1.3.7.0、日英MSI 1.3.7。ProductCodeとPackageCodeを更新し、UpgradeCodeを維持する。

## 検証
- Releaseビルド成功。ソースからの自動テスト462件すべて成功。
- 日本語・英語MSIから抽出した各本体でも、それぞれ462件すべて成功。
- 両本体のバージョン1.3.7.0とWPF画面リソースを確認。
- 両MSIのProductVersion 1.3.7、言語1041／1033、更新用識別子と終了処理順序を確認。
- 配布物11件のSHA-256と、両ZIPに含まれるMSI・起動用EXE・LICENSEの一致を確認。
- 日本語MSIは公開前に再生成。英語MSIはVisual Studioの再起動要求により再生成できなかったため、既存の検証済みMSIを使用。本体の全1,564メソッドのILが現在のソースから再構築した本体と一致し、抽出本体の全462件のテストも再確認した。
- 実環境への通常インストール・更新と実Explorerでの手動操作は未検証。

## 配布物
- artifacts/releases/v1.3.7.0/KjTabBar-v1.3.7.0-ja.zip
- artifacts/releases/v1.3.7.0/KjTabBar-v1.3.7.0-en.zip
- ZIP内のsetup.exeとSetup.msiを同じフォルダーに展開して使用する。

## 検証の範囲
- 自動テスト・模擬Explorer・WPF Dispatcher・MSI内容検査と、実Explorer画面操作・通常インストール／更新・長時間運用は区別する。
- 実環境へのインストールは行っていない。
